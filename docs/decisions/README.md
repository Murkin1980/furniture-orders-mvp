# Decisions

Architecture and product decision records for the furniture platform.

Keep new long-form `*_DECISION.md` documents here instead of the repository
root. Short entrypoint documents such as `PRODUCT.md`, `DESIGN.md`,
`DATA_SOURCES.md`, and `LIVE_SITES.md` stay in the root because agents and
reviewers read them before most sessions.

