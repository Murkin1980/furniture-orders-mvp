# Converted Markdown

Store reviewed MarkItDown output here. Every converted document must retain its source reference in `DATA_SOURCES.md`.

Review tables, numbers, legal wording, and headings before using the result as AI context.
