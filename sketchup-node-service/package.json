{
  "name": "furniture-sketchup-node-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/config.js && node --check src/server.js && node --check src/execution-adapter.js && node --check src/file-queue-executor.js && node --check src/runtime.js",
    "test": "node --test"
  }
}
